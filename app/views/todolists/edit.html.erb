<h1>Editing Book</h1>
<%= form_with model:@list, url:"/todolists/#{@list.id}", local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit :'Update Book' %>

<% end %>